# Copy this file to .env.local and fill in what you need.

# The contact form posts to this Express backend (see the backend/ folder).
# Locally this is http://localhost:4000; once deployed, set it to your
# backend's live URL (e.g. Render).
NEXT_PUBLIC_API_URL=http://localhost:4000
